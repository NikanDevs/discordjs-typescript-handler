import { interactionOptions } from '../../typings';

// @ts-ignore
export const repeatContextmenu = {
	name: 'Repeat',
	description: 'Repeats the selected message.',
	directory: 'utility',
	cooldown: 3000,
	permission: [],
	type: 3,
} as interactionOptions;

